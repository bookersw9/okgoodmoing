apiVersion: v1
kind: Service
metadata:
  name: newsd
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "newsd"
    port: 8080
  selector:
    app: newsd
